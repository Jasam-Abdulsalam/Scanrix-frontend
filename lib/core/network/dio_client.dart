// TODO: set up the shared Dio instance here (base options, interceptors for
// the JWT bearer token / refresh, logging) once the http client package is
// decided. Domain use cases will import and call this.
